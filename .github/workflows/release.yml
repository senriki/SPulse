name: Release

on:
  push:
    tags:
      - 'v*'
      - '!v*-*'   # exclude pre-release suffixes (v1.0.0-rc.1 etc.) — those go through release-rc.yml

jobs:
  build:
    uses: ./.github/workflows/build-release.yml
    with:
      prerelease: false
